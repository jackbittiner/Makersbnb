<html>
  <body>

    <%if params[:username]%>
      <p><%=params[:username]%>'s spaces</p>
    <% end %>

    <form method="get" action="/spaces/new">
      <button type='submit' class="btn btn-primary">List a Space</button>
    </form>

    <div class="row">
      <ul id="spaces">
        <% @spaces.each do |space| %>
        <div class='listing'>
          <a href="/spaces/<%= space.id %>">
          <h2 align='left'><%= space.name %></h2>
          <img src="<%= space.image_url %>" width="150" height="150" align='right'>
          </a>
          <p><%= space.description %></p>
          <p>£<%= space.price %></p>
          <p><%= space.available_from %></p>
          <p><%= space.available_to %></p>
        </div>
        <% end %>
      </ul>
    </div>
  </body>
</html>
